upstream es-pd {
    server es-pd:4112;
}
server {
    listen 4112;
    location / {
        proxy_set_header    X-Real-IP $remote_addr;
        proxy_set_header    Host      $http_host;
        proxy_pass          http://es-pd;
    }
}
